---
description: UI/UX Pro Max — design intelligence (67 styles, 96 palettes, 57 font pairings, 13 stacks)
argument-hint: [design or UI/UX task]
---
Use the **ui-ux-pro-max** skill (via the Skill tool) to handle the following UI/UX task.

Run its design-system search engine under the hood as the skill instructs
(`python scripts/search.py "<query>" --design-system ...` from
`~/.claude/skills/ui-ux-pro-max/`), then synthesize the recommendations and apply
them to the user's project.

Task:
$ARGUMENTS
