# Admitica — дизайн-система

> Источник правды: `full code/app/src/index.css` (Tailwind 4 `@theme` + CSS-переменные).
> Тёмная тема — **по умолчанию**; светлая включается атрибутом `[data-theme="light"]` на `<html>`.
> Акцент проекта — **бирюза** (teal/turquoise): ярко на тёмном, темнее на светлом ради читаемости.

---

## Типографика

- **Шрифт:** **Inter**
- **Стек:** `"Inter", "Inter Fallback", system-ui, -apple-system, "Segoe UI", sans-serif`
- Сглаживание: antialiased, `text-rendering: optimizeLegibility`.

---

## Цветовые токены

| Токен | 🌙 Тёмная (default) | ☀️ Светлая | Назначение |
|-------|------------------|-----------|-----------|
| **Поверхности** | | | |
| `--color-bg` | `#0a0c0c` | `#f3f7f6` | фон страницы |
| `--color-surface` | `#101414` | `#ffffff` | поверхности |
| `--color-card` | `#151a1a` | `#ffffff` | карточки |
| `--color-card-2` | `#1b2121` | `#e9f1ef` | вложенные карточки |
| **Границы** | | | |
| `--color-border` | `rgba(255,255,255,0.08)` | `rgba(12,40,36,0.10)` | обычные рамки |
| `--color-border-strong` | `rgba(255,255,255,0.14)` | `rgba(12,40,36,0.18)` | акцентные рамки |
| **Текст** | | | |
| `--color-fg` | `#f4f7f6` | `#0d1a18` | основной текст |
| `--color-fg-muted` | `#99a3a1` | `#4c5a58` | приглушённый текст |
| `--color-fg-faint` | `#5e6766` | `#7a8987` | едва заметный текст |
| **Бирюза (акцент)** | | | |
| `--color-accent` | `#2dd4bf` | `#0d9488` | заливки: кнопки, активные пилюли |
| `--color-accent-soft` | `rgba(45,212,191,0.12)` | `rgba(13,148,136,0.12)` | мягкая подложка под акцент |
| `--color-accent-fg` | `#04201d` | `#ffffff` | текст НА заливке (semibold) |
| `--color-accent-text` | `#2dd4bf` | `#0f766e` | бирюзовый текст/иконки на фоне |
| `--color-accent-glow` | `rgba(45,212,191,0.30)` | `rgba(13,148,136,0.18)` | свечения/тени |
| **Семантика** | | | |
| `--color-positive` | `#4ade80` | `#16a34a` | успех / положительное |
| `--color-warning` | `#fbbf24` | `#d97706` | предупреждение |
| `--color-danger` | `#f87171` | `#dc2626` | ошибка / опасность |
| `--color-danger-fg` | `#2a0a0a` | `#ffffff` | текст НА danger-заливке |

---

## Правила использования цвета (читаемость)

1. **Бирюзовый текст/иконки** → `--color-accent-text` (НЕ `--color-accent`). В светлой теме это более тёмный оттенок (`#0f766e`) для контраста AA на белом.
2. **Заливки** (кнопки, активные пилюли) → `--color-accent`; текст на них → `--color-accent-fg`, метки **semibold**.
3. **Свечения/тени** → `--color-accent-glow`; в светлой теме приглушены.
4. **Danger-заливка**: текст из `--color-danger-fg` (в тёмной теме danger — светло-красный, поэтому белый текст на нём не читается).

---

## Производные и точечные цвета (не токены)

- **Белый блик hero-glow** (только тёмная): `rgba(255,255,255,0.05)` — мягкое радиальное свечение за заголовком.
- **Тень белых карточек** (только светлая): `0 1px 2px rgba(13,26,24,0.04)` + `0 12px 32px -20px rgba(13,26,24,0.18)`.
- **Трек range-слайдеров** (GPA / бюджет / стоимость): `color-mix(in srgb, var(--color-fg) 8%, transparent)`.
- **Фокус-кольцо слайдера**: `color-mix(in srgb, var(--color-accent) 60%, transparent)`.

---

## Скругления (radii)

| Токен | Значение |
|-------|----------|
| `--radius-lg` | `0.75rem` |
| `--radius-xl` | `1rem` |
| `--radius-2xl` | `1.25rem` ← **дефолт продукта** (`rounded-2xl`) |
| `--radius-3xl` | `1.75rem` |

---

## Движение (motion)

- **Easing:** `--ease-out-soft: cubic-bezier(0.16, 1, 0.3, 1)`
- **Длительности:** `--duration-fast: 200ms` · `--duration-base: 250ms` · `--duration-slow: 300ms`
- Анимировать только `transform` и `opacity` (GPU). Уважать `prefers-reduced-motion` (анимации гасятся).

---

## Цвет программ (вне дизайн-системы)

У каждого вуза/гранта/стажировки есть собственный цвет аватара (`color`), который лежит **в данных** (`data/programs.js`), генерится из небольшой палитры по программе. Это цвет кружка/плитки конкретной карточки, **не часть темы** — менять палитру тем его не затрагивает.
